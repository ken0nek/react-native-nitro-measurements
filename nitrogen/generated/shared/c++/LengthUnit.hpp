///
/// LengthUnit.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitromeasurements {

  /**
   * An enum which can be represented as a JavaScript union (LengthUnit).
   */
  enum class LengthUnit {
    METERS      SWIFT_NAME(meters) = 0,
    KILOMETERS      SWIFT_NAME(kilometers) = 1,
    CENTIMETERS      SWIFT_NAME(centimeters) = 2,
    MILLIMETERS      SWIFT_NAME(millimeters) = 3,
    MILES      SWIFT_NAME(miles) = 4,
    YARDS      SWIFT_NAME(yards) = 5,
    FEET      SWIFT_NAME(feet) = 6,
    INCHES      SWIFT_NAME(inches) = 7,
    NAUTICALMILES      SWIFT_NAME(nauticalmiles) = 8,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitromeasurements

namespace margelo::nitro {

  // C++ LengthUnit <> JS LengthUnit (union)
  template <>
  struct JSIConverter<margelo::nitro::nitromeasurements::LengthUnit> final {
    static inline margelo::nitro::nitromeasurements::LengthUnit fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("meters"): return margelo::nitro::nitromeasurements::LengthUnit::METERS;
        case hashString("kilometers"): return margelo::nitro::nitromeasurements::LengthUnit::KILOMETERS;
        case hashString("centimeters"): return margelo::nitro::nitromeasurements::LengthUnit::CENTIMETERS;
        case hashString("millimeters"): return margelo::nitro::nitromeasurements::LengthUnit::MILLIMETERS;
        case hashString("miles"): return margelo::nitro::nitromeasurements::LengthUnit::MILES;
        case hashString("yards"): return margelo::nitro::nitromeasurements::LengthUnit::YARDS;
        case hashString("feet"): return margelo::nitro::nitromeasurements::LengthUnit::FEET;
        case hashString("inches"): return margelo::nitro::nitromeasurements::LengthUnit::INCHES;
        case hashString("nauticalMiles"): return margelo::nitro::nitromeasurements::LengthUnit::NAUTICALMILES;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum LengthUnit - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitromeasurements::LengthUnit arg) {
      switch (arg) {
        case margelo::nitro::nitromeasurements::LengthUnit::METERS: return JSIConverter<std::string>::toJSI(runtime, "meters");
        case margelo::nitro::nitromeasurements::LengthUnit::KILOMETERS: return JSIConverter<std::string>::toJSI(runtime, "kilometers");
        case margelo::nitro::nitromeasurements::LengthUnit::CENTIMETERS: return JSIConverter<std::string>::toJSI(runtime, "centimeters");
        case margelo::nitro::nitromeasurements::LengthUnit::MILLIMETERS: return JSIConverter<std::string>::toJSI(runtime, "millimeters");
        case margelo::nitro::nitromeasurements::LengthUnit::MILES: return JSIConverter<std::string>::toJSI(runtime, "miles");
        case margelo::nitro::nitromeasurements::LengthUnit::YARDS: return JSIConverter<std::string>::toJSI(runtime, "yards");
        case margelo::nitro::nitromeasurements::LengthUnit::FEET: return JSIConverter<std::string>::toJSI(runtime, "feet");
        case margelo::nitro::nitromeasurements::LengthUnit::INCHES: return JSIConverter<std::string>::toJSI(runtime, "inches");
        case margelo::nitro::nitromeasurements::LengthUnit::NAUTICALMILES: return JSIConverter<std::string>::toJSI(runtime, "nauticalMiles");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert LengthUnit to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("meters"):
        case hashString("kilometers"):
        case hashString("centimeters"):
        case hashString("millimeters"):
        case hashString("miles"):
        case hashString("yards"):
        case hashString("feet"):
        case hashString("inches"):
        case hashString("nauticalMiles"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
